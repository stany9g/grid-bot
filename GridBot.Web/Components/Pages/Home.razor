@page "/"
@inject NavigationManager Navigation

<PageTitle>ALTE Dashboard</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to the MudBlazor Dashboard
        Navigation.NavigateTo("/dashboard", replace: true);
    }
}
